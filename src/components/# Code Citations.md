# Code Citations

## License: MIT
https://github.com/pjfordham/processing_cpp/blob/93144adaf41180ea8df0f5755d843dd20c2fd0ba/examples/Pete/data/ShaderArt.glsl

```
0;
  
  for(float i = 0.0; i < 4.0; i++) {
    uv = fract(uv * 1.5) - 0.5;
    
    float d = length(uv) * exp(-length(uv0));
    vec3 col = palette(length(uv0) + i * 0.4 + iTime * 0.4);
    
    d = sin(d * 8.0 + iTime) / 8.0;
    d = abs(d);
    d = pow(0.01 / d, 1.2);
    
    finalColor += col * d
```


## License: unknown
https://github.com/Haldir65/androidMedia/blob/69c594e0e24cd2392d5ae90b3c22986b04de932f/DroidMedia/medialibs/filterLibrary/src/main/assets/shadertoy/fragment_shader_llgxwc2.glsl

```
0;
  
  for(float i = 0.0; i < 4.0; i++) {
    uv = fract(uv * 1.5) - 0.5;
    
    float d = length(uv) * exp(-length(uv0));
    vec3 col = palette(length(uv0) + i * 0.4 + iTime * 0.4);
    
    d = sin(d * 8.0 + iTime) / 8.0;
    d = abs(d);
    d = pow(0.01 / d, 1.2);
    
    finalColor += col * d
```


## License: unknown
https://github.com/Defiant-System/textedit/blob/16df3abed3f37ed0d85f88ddbee05deab8368cd0/public/samples/shader.y

```
0;
  
  for(float i = 0.0; i < 4.0; i++) {
    uv = fract(uv * 1.5) - 0.5;
    
    float d = length(uv) * exp(-length(uv0));
    vec3 col = palette(length(uv0) + i * 0.4 + iTime * 0.4);
    
    d = sin(d * 8.0 + iTime) / 8.0;
    d = abs(d);
    d = pow(0.01 / d, 1.2);
    
    finalColor += col * d
```


## License: unknown
https://github.com/planetis-m/raylib-examples/blob/d4cad82e96835f11872ea357fec69a1fdc7d3b9b/personal/shaderart.nim

```
0;
  
  for(float i = 0.0; i < 4.0; i++) {
    uv = fract(uv * 1.5) - 0.5;
    
    float d = length(uv) * exp(-length(uv0));
    vec3 col = palette(length(uv0) + i * 0.4 + iTime * 0.4);
    
    d = sin(d * 8.0 + iTime) / 8.0;
    d = abs(d);
    d = pow(0.01 / d, 1.2);
    
    finalColor += col * d
```


## License: MIT
https://github.com/inhopp/inhopp.github.io/blob/0d4f233a34b47a6affe39dd3ccd6d511fa89657c/_posts/Graphics/Graphics/2023-08-09-graphics37.md

```
0;
  
  for(float i = 0.0; i < 4.0; i++) {
    uv = fract(uv * 1.5) - 0.5;
    
    float d = length(uv) * exp(-length(uv0));
    vec3 col = palette(length(uv0) + i * 0.4 + iTime * 0.4);
    
    d = sin(d * 8.0 + iTime) / 8.0;
    d = abs(d);
    d = pow(0.01 / d, 1.2);
    
    finalColor += col * d
```


## License: unknown
https://github.com/ugurctaygun/glsl-shaders/blob/d250934a4081255152fb9b4329e1c5222e51209f/src/Shaders/ShapeAnimation.jsx

```
0;
  
  for(float i = 0.0; i < 4.0; i++) {
    uv = fract(uv * 1.5) - 0.5;
    
    float d = length(uv) * exp(-length(uv0));
    vec3 col = palette(length(uv0) + i * 0.4 + iTime * 0.4);
    
    d = sin(d * 8.0 + iTime) / 8.0;
    d = abs(d);
    d = pow(0.01 / d, 1.2);
    
    finalColor += col * d
```

